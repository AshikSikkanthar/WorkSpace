import React from 'react'

function MemoReact({name}) {
    console.log(' memo function based component got rendered',name)
    return (
        <div>
            <h1  class="text-info" >This is function based memo  component : {name}</h1>
        </div>
    )
}

export default React.memo(MemoReact)